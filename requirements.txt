fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9
sqlalchemy>=2.0
anthropic>=0.40
httpx>=0.27
pypdf>=5.0
python-docx>=1.1
pyyaml>=6.0
python-dotenv>=1.0
pytest>=8.0

# Optional — offline OCR for resume photos (Windows only; no API key needed).
# On other platforms, image upload falls back to Claude vision (needs a key).
Pillow>=10.0
winocr>=0.0.15; sys_platform == "win32"
